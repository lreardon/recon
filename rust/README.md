# Shoot I never code in Rust!

## `cargo build` / `cargo run`

`build` compiles, `run` builds and compiles.


## Jumping back in

Run the `read_fst` program via `cargo run --bin load_evaluations_fst`.

That ought to be enough to get you back up to speed.